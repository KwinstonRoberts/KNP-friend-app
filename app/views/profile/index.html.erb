<% content_for :nav_assessment_class do %>
  <%= assessment_css(@assessment) %>
<% end %>

<div class="profileWrapper <%= assessment_css(@assessment) %>">
<div class="inner">
    <div class="typeWrapper">

       <% assessment = @assessment.personality_types.first %>
       <% trait = @traits.first %>
       <h1><%= @user.name %></h1>
       <img src='<%= assessment.personality_type.badge.image_large %>'>
       <h2> Personality Type </h2>
       <h3><%= assessment.personality_type.name %></h3>
       <h4><%= assessment.score %></h4>
       <h4><%= assessment.personality_type.description %></h4>

       <h4> Famous person with same personality - <%= assessment.personality_type.famous_people.first.name %></h4>

       <h2> Personality Trait </h2>
       <h3> <%= trait.personality_trait.name%>  </h3>
       <h4> <%= trait.score%> </h4>
       <h4> <%= trait.personality_trait.definition%> </h4>
       <h4> <%= trait.personality_trait.description%> </h4>
    </div>

    <div class="activityWrapper">
      <h2>Activities for <%= assessment.personality_type.name %></h2>
        <% @activities.each do |activity| %>
          <% if activity.personality_type == assessment.personality_type.name %>



        <section title=".squaredTwo">
    <!-- .squaredTwo -->


          <div class="checkContainer">
            <div class="squaredTwo">
              <%= check_box_tag activity.id %>
              <%= label_tag activity.id, "" %>
            </div>
            <span class="checkLabel"><%= activity.name %></span>
          </div>


    <!-- end .squaredTwo -->
      </section>


          <% end %>
      <% end %>
    </div>
  </div>

  <div class="matchesWrapper">
    <div class="headline">

      <h2> Matches </h2>
    </div>

        <% if @matches %>
        <% @matches.each do |match| %>
        <div class="matchesInner">
          <h3><%= match[:name] %></h3>
          <img src='<%=match[:result]%>'>
          <p></p>
        </div>

        <%end%>
      <%end%>




</div>
</div>

